<%= content_for :page_title, "Admin" %>
<%= content_for :sub_title, "New admin" %>
<div class="row">
  <div class="col-md-12">
    <%= form_for(@admin, url: admins_admins_path, class: "form-horizontal", id: "new-admin") do |f| %>
      <%= render "form", f: f %>
    <% end %>
  </div>
</div>
