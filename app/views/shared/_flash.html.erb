<% flash.each do |message_type, message| %>
  <% if message %>
    <div class="alert alert-<%= message_type %> show fade">
      <p class="text-center"><%= message %></p>
      <a class="close" data-dismiss="alert" href="#" aria-hidden="true">&times;</a>
    </div>
  <% end %>
<% end %>
